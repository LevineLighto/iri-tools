import { Paddings } from "@/config/classes/Paddings";
import { Radius } from "@/config/classes/Radius";

export const BasicCardClasses = {
    background  : 'bg-white',
    text        : 'text-gray-950',
    radius      : Radius.large.all,
    shadow      : 'shadow-lg',
    padding     : Paddings.lg,
    position    : 'relative',
}